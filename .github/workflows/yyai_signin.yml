name: 边界AI每日签到

on:
  schedule:
    # 每天北京时间 08:05 执行（UTC 00:05）
    - cron: '5 0 * * *'
    # 备用：北京时间 10:05（UTC 02:05）
    - cron: '5 2 * * *'
  workflow_dispatch:

jobs:
  signin:
    name: 边界AI签到
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 配置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 安装依赖
        run: npm install

      - name: 执行边界AI签到
        env:
          YYAI_TOKEN: ${{ secrets.YYAI_TOKEN }}
          YYAI_ACCESS_TOKEN: ${{ secrets.YYAI_ACCESS_TOKEN }}
          YYAI_UID: ${{ secrets.YYAI_UID }}
          QQ_MAIL_USER: ${{ secrets.QQ_MAIL_USER }}
          QQ_MAIL_PASS: ${{ secrets.QQ_MAIL_PASS }}
        run: node src/tasks/yyai_signin.js
